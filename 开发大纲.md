# My IDE 开发大纲

> 本文件是项目的最高指导文档。**任何新功能、任何代码改动，都必须先对照本大纲。**

## 一、项目愿景

**为什么不用 PyCharm：** 内存占用太高（常驻 1GB+），绝大多数功能用不上，却为它们付出性能和复杂度代价。

**My IDE 的定位：** 提供 PyCharm 的核心编辑体验（文件树、多标签、Markdown/HTML 渲染、Git 日志/提交/对比），但——

1. **只包含我要的功能**，不堆砌用不上的东西
2. **性能占用远低于 PyCharm**（内存、CPU、启动速度）
3. **可定制**：快捷键、布局、行为都贴合我的使用习惯
4. **可扩展**：未来想加什么，通过插件机制加，不污染核心

## 二、核心原则（优先级从高到低）

| # | 原则 | 说明 |
|---|------|------|
| 1 | **极简优先** | 加功能前先问：真的需要吗？能砍则砍。每加一个功能，必须有一个"省掉什么"的理由 |
| 2 | **性能敏感** | 一切以低内存、低延迟为准。任何操作不允许"卡一下" |
| 3 | **可扩展** | 新格式渲染、新功能一律走插件机制，核心代码保持小而稳 |
| 4 | **可定制** | 快捷键、显示、行为可调，界面中文 |
| 5 | **个人优先** | 只服务我自己的需求，不为通用性妥协 |

## 三、功能边界

### ✅ 已实现（v0.1）
- 文件树：懒加载、右键菜单、单击文件复制完整路径
- 多标签页 + 文本/代码编辑保存（Ctrl+S）
- 渲染：Markdown（代码高亮）、HTML（沙箱预览）、CSV（示例插件）
- Git（纯 JS，无需安装 git）：日志、Ctrl+K 提交面板（勾选文件/amend）、修改前后左右分栏对比
- PyCharm 式工具窗口：项目 / 大纲 / Git 按钮切换（Ctrl+1/2/3）
- Markdown 大纲：标题层级 + 点击跳转
- 快捷键体系 + 一键启动脚本

### 🚧 规划中（v0.2+，按需排期）
- 主题切换（深色/浅色）
- diff 视图可折叠 hunk
- 快速打开文件（Ctrl+P）、内容搜索
- 图片 / PDF / drawio 等格式渲染插件
- 会话记忆（上次打开的标签页）

### ❌ 明确不做（保持轻量）
- 完整语言服务（代码补全、跳转、重构、语法检查）——这是 PyCharm 内存大户，个人项目不需要
- 远程开发、数据库工具、部署集成
- 多语言国际化（界面只做中文）
- 多用户/账号体系

## 四、性能基线（验收标准）

| 指标 | 目标 | 说明 |
|------|------|------|
| 常驻内存 | **< 200 MB** | PyCharm 常驻 1GB+，这是最大卖点 |
| 启动到可交互 | **< 2 秒** | 冷启动 |
| 打开文件 | **< 0.5 秒** | 10MB 以内文本 |
| Git 状态扫描 | **< 1 秒** | 1 万个文件的仓库 |
| 空载 CPU | **≈ 0%** | 挂着不动不吃 CPU |

**已落实的性能工作：**
- Git 状态：`statusMatrix` 一次 walk 批量计算（原来每个文件重新解析整棵树，大仓库卡死）
- Diff：4000 行阈值保护，超大文件线性降级，杜绝 O(N²) 卡死
- 弹窗监听器一次性注册，杜绝内存泄漏
- 修复 isomorphic-git `findRoot` 的 Windows 路径 bug（误判仓库）

## 五、扩展机制（插件 API）

```js
// 渲染器插件：plugins/ 目录放一个 JS 文件即可
api.registerRenderer(['pdf', 'drawio'], ({ path, name, ext, content }) => {
  return domElement; // 或返回字符串作为源码
});
```

未来扩展点：
- **动作插件**：注册自定义快捷键 + 命令
- **主题插件**：切换配色
- **文件处理器插件**：自定义打开/保存行为
- 插件失败不影响主程序，加载顺序：内置渲染器 → 用户插件（后注册优先）

## 六、架构约束

- 主进程（main.js）只做系统能力：文件系统 / Git / 剪贴板 / 窗口；UI 全部在渲染进程
- `git-service.js` 保持**纯 Node**（不依赖 Electron），可独立测试
- 渲染层模块互相解耦：tree / viewer / outline / git-panel / shortcuts，通过 window.App 协作
- **任何新功能必须带测试**（tests/git.test.js 或 tests/dom.test.js），`npm test` 必须全绿

## 七、版本节奏

- 每个版本只做 1-2 个功能，做完、测完、用顺了再进下一个
- 重大改动先写进本大纲，再动代码1