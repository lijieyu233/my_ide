# TODO（按时间划分）

## 2026-08-22（第五轮 · 已完成）

### Bug 修复
- [x] 自定义主题调整没有用（根因：覆盖层变量写 documentElement，被 body.theme-light/pink/crimson 选择器上的变量定义覆盖）→ 改写 body inline style，所有主题下调色立即生效
### 功能增强
- [x] 调整项目从 8 项扩到 17 项，按 4 组渲染：基础配色（强调色/成功/危险）、背景（编辑区/侧栏/悬停/选中/输入框/弹窗）、文字（正文/标题/弱化/编辑器/行内代码）、边框/控件（边框线/中边框/按钮边框）
- [x] UI 动态自定义主题：设置 → 主题新增「动态自定义」表单，核心颜色叠加在任意预设主题上实时生效
  - [x] 逐项取色即时生效 + 持久化（重启保持，切换预设主题后继续叠加）
  - [x] 逐项恢复 × / 全部恢复主题默认
  - [x] 旧版 myide-accent 强调色数据自动迁移
- [x] 自定义主题的添加 / 删除：调好颜色后「💾 保存为自定义主题」命名保存为主题卡片，点击卡片应用，卡片右上角 × 确认删除；删除当前主题自动回退其基础主题
  - [x] Modal 改为面板栈：设置页内可再弹命名/确认弹窗（此前会互相顶掉）
- [x] 背景透明度滑条移到状态栏最右端（字号控件右侧）：设置背景图后显示，拖动即时生效，与设置页滑条双向同步

## 2026-08-21（第四轮反馈 · 已完成）

### Bug 修复
- [x] live 实时预览与 preview 预览样式显示不同：装饰样式与 .md-view 渲染 CSS 系统性对齐（标题字号/边框、行内代码、引用、围栏代码块同源变量）
- [x] Ctrl+B 收起侧栏不起效（根因：快捷键从未注册）→ 改为 Ctrl+`，按钮移到工具条顶部
- [x] 文件拖拽：拖到文件夹行下面（文件行）也能移入其所在目录（dropDirOf）
- [x] 复制多个文件粘贴：剪贴板 CF_HDROP/FileNameW 多文件读写 + 循环粘贴全部

### 功能增强
- [x] 上下移动切换项扩展到所有需要的地方：大纲 / Git 面板（本地修改 + 提交历史）↑↓ 选择 + Enter 打开
- [x] 背景图可选图片显示的区域：显示方式（铺满/完整/平铺）+ 九宫格位置，设置页外观区配置
- [x] 右下角显示一个玩宠：SVG 小橘猫，toast 消息由它播报（头顶气泡 + 说话跳跃动画），点击卖萌

### UI / 主题优化
- [x] 收起侧栏按钮放上面（工具条顶部第一位）
- [x] 主题样式自定义入口明确化：设置 → 主题，新增自定义强调色（color picker + 恢复默认，全主题生效）
- [x] 左侧和右侧显示区域的中间线不要做成透明的：#sidebar-resizer 背景改实色 var(--border)

## 2026-08-21（第三轮 · 修改建议二 · 已完成）

### Bug 修复
- [x] md 大纲点击跳转位置不对：解析跳过代码块内 # 行；live/source 走 CM gotoLine，split/preview 滚动标题
- [x] HTML 预览不显示样式：srcdoc 注入 `<base>` + sandbox 加 allow-same-origin 解析相对路径
- [x] HTML 点击「查看源码」后有「◉ 预览」按钮可切回
- [x] HTML 预览顶部「OCTYPE html>」：按键转发脚本移到文档末尾
- [x] HTML 预览时 Ctrl+1/2/3 失效：iframe 内按键 postMessage 转发回主窗口
- [x] 编辑器内 Ctrl+C 复制文本触发文件复制：快捷键豁免补上 contenteditable（CM6）
- [x] 关闭项目后目录树清理：关闭当前项目自动切到剩余项目，一个不剩则清空回空状态
- [x] 部分 py 文件误判二进制：UTF-16 无 BOM 零字节奇偶位启发式修复
- [x] 打开文件夹失败：dialog 链路复验通过（IPC/preload/renderer 完整，取消返回 null）

### 功能增强
- [x] Delete 键删除文件（文件树选中时）
- [x] Ctrl+X 剪切文件（粘贴即移动，写系统剪贴板）
- [x] 目录树右键「作为项目打开」
- [x] Markdown 模式全局统一：localStorage 记住模式，切换标签/新开文件沿用

### UI / 主题优化
- [x] 侧边栏宽度拖动不流畅：localStorage 持久化移到 mouseup，拖动过程只改宽度

## 2026-08-21（第二轮 · 修改建议 / 优化建议三 · 已完成）

### Bug 修复
- [x] 目录树实时同步：外部增删改文件/文件夹后，目录树不能及时刷新
- [x] Markdown 显示窗口存在显示问题
- [x] HTML「在浏览器中打开」无效果
- [x] 右键菜单「在文件夹中显示」改为「在资源管理器中显示」
- [x] 分屏的源码区还是有问题，上面一片空白
- [x] 状态栏显示「main · 117 处修改」：dist/ 构建产物未忽略，被算进 Git 修改
- [x] 搜索没有高亮：搜索结果的匹配文本未高亮显示
- [x] 顶部搜索按钮样式在部分主题下不一致
- [x] 实时预览看起来和源码一样：live 模式装饰乱序异常被吞，改 Decoration.set 排序修复
- [x] 背景图问题：透明度滑块调不动；加图片后界面布局出错；只有左边区域能看到图片
- [x] 文档开头字符消失/乱码（BOM）：BOM 误切导致，已按真实 BOM 判断
- [x] 点击文件后目录树上下跳动：reveal 已改为仅在目标行不可见时滚动

### 功能增强
- [x] Markdown 预览与编辑：Obsidian 式实时预览编辑模式（CM6 引擎）
  - [x] M1：CodeMirror 6 vendor 化 + 源码模式接入
  - [x] M2：Live Preview 装饰层（光标行源码 / 其余行渲染）
  - [x] P1：格式快捷键（Ctrl+B/I/K）、列表续行、wiki 链接补全
- [x] 文件拖拽移动：支持通过拖动调整文件/文件夹位置
- [x] 多选文件：Ctrl+点击（多选）、Shift+点击（范围选择）
- [x] 顶部项目栏支持拖动调整顺序
- [x] 标签栏收纳：顶部项目栏与右侧文件栏打开过多时自动合并（下拉列表）
- [x] 背景图设置：设置页支持配置界面背景图
- [x] 分区域字体大小：编辑区 / 界面分别调整字号
- [x] 文件树键盘导航：上下左右移动选中项（上下换选中、左右展开/收起）
- [x] 主题样式自定义：通过 UI 灵活设置主题样式（主色/背景色等）
- [x] 自动保存：停止输入 3 秒写盘 + 切换/关闭静默保存
- [x] Ctrl+Z 撤销文件操作（新建/重命名/删除/移动/粘贴）
- [x] 复制粘贴文件：Ctrl+C / Ctrl+V，写系统剪贴板（FileNameW/CF_HDROP，可在资源管理器粘贴）
- [x] 视频/音频展示

### UI / 主题优化
- [x] 深色主题优化：黑色偏泛白 → 已加深为纯正深色
- [x] 去掉 Windows 原生顶部窗口栏（自绘标题栏，可拖拽窗口）
- [x] 新增粉红色主题（粉红=浅粉，深红独立保留）
- [x] 关闭项目不需要确认（切换/移除项目免弹窗）
- [x] 打开文件夹改为打开项目
- [x] 最顶部的复制路径和刷新按钮去掉
- [x] 字号调整：快捷键只调文档显示区字号（侧栏目录区不随动）
- [x] 状态栏显示当前字号，并提供调整字号的 UI（+/- 按钮）
- [x] 底部字号调整控件移到状态栏右侧
- [x] 顶部项目路径移到目录树根目录右侧（从工具栏移除）
- [x] 顶部 Git 状态显示去掉（状态栏保留分支信息）
- [x] 左侧边栏收起/展开按钮（Ctrl+B）
- [x] 保存按钮去掉（已全部自动保存）
- [x] 按钮图标美化（统一图标风格）

### 性能优化
- [x] 定位卡顿/卡死问题：使用日志 + 耗时埋点 + 错误自动捕获 + 日志轮转
- [x] 启动速度优化：CM6 延迟加载 + 主进程瘦身
