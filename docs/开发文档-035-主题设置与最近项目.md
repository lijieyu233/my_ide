# 开发文档 035 —— 设置页主题分类 + 启动页最近项目

> 状态：已完成 · 补全占位

## 一、内容

- 设置页「🎨 主题」分类（原为"敬请期待"占位）：深色/浅色选择，即时生效 + 持久化
- 无标签空状态显示**最近打开的项目**按钮，点击即切换（启动后快速回到常用项目）

## 二、实现

- Theme.set(t)：显式设置主题（apply + localStorage）
- settings.js renderTheme()：两个选项按钮高亮当前
- App.getProjects() + 空状态最近项目列表（每次项目变更后刷新）

## 三、验收

- [x] npm test 全绿
- [x] 无新依赖