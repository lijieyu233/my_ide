# 开发文档 036 —— Git 修改按目录分组 + Toast 上限

> 状态：已完成 · 体验打磨

## 一、需求

- 本地修改文件多且跨目录时平铺难找——按**顶层目录分组**，组可折叠
- 连续错误时 toast 无限堆积——**上限 5 条**，超出移除最旧

## 二、实现

- git-panel render 本地修改：按路径首段分组（根目录文件归「根目录」），组标题显示数量、可点击折叠
- MI.toast：容器超过 5 条时移除最旧

## 三、验收

- [x] npm test 全绿
- [x] 无新依赖